.DS_Store
# Env files (keep only examples)
.env
.env.local
.env.*
!.env.example
!.env.*.example

# Dev data owner file: a developer's real app_user + user_identity (see backend/src/features/dev-data)
.devdata-owner

# Personal shell config (copy from .devcontainer/bashrc.example to ~/.bashrc in the container)
.bashrc
.bashrc.local
.pnpm-store
node_modules
.devcontainer/.deps-installed

# Test coverage
coverage/

# Integration test artifacts
integration/node_modules/
integration/test-results/
integration/playwright-report/
integration/.playwright/
integration/.cache/