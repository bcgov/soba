{{- if .Values.frontend.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "soba.fullname" . }}-frontend
  labels:
    {{- include "soba.labels" (dict "root" . "component" "frontend") | nindent 4 }}
  {{- with .Values.frontend.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.frontend.ingress.ingressClassName }}
  ingressClassName: {{ .Values.frontend.ingress.ingressClassName }}
  {{- end }}
  {{- if .Values.frontend.ingress.tls }}
  tls:
    - hosts:
        - {{ include "soba.frontendHost" . }}
      {{- if .Values.frontend.ingress.tlsSecretName }}
      secretName: {{ .Values.frontend.ingress.tlsSecretName }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ include "soba.frontendHost" . }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "soba.fullname" . }}-frontend
                port:
                  name: http
{{- end }}
