**.next
# Secondary dev-server build dir (forms/submit-mode via NEXT_DIST_DIR)
.next-forms
.env
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local
node_modules
*.tsbuildinfo
# Next generates this; its content is distDir-dependent, so it churns between the
# designer/forms dev servers. Untracked (Next recreates it on dev/build).
next-env.d.ts