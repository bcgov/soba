# Frontend environment variables — localhost / local dev
# Values below are for running against local services (Form.io, Keycloak dev).
# Created from this file when the devcontainer starts; not committed.

NEXT_PUBLIC_FORMIO_BASE_URL=http://localhost:3001
# BC Gov dev Keycloak
NEXT_PUBLIC_KEYCLOAK_URL=https://dev.loginproxy.gov.bc.ca/auth
NEXT_PUBLIC_KEYCLOAK_REALM=standard
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=connected-services-submit-6349